<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.FireReportRecordMapper">
    
    <resultMap type="FireReportRecord" id="FireReportRecordResult">
        <result property="reportId"    column="report_id"    />
        <result property="taskId"    column="task_id"    />
        <result property="taskName"    column="task_name"    />
        <result property="reportName"    column="report_name"    />
        <result property="filePath"    column="file_path"    />
        <result property="fileSize"    column="file_size"    />
        <result property="planStartTime"    column="plan_start_time"    />
        <result property="planEndTime"    column="plan_end_time"    />
        <result property="managerName"    column="manager_name"    />
        <result property="operatorNames"    column="operator_names"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectFireReportRecordVo">
        select r.report_id,
               r.task_id,
               ifnull(nullif(r.task_name, ''), convert(t.task_name using utf8mb4) collate utf8mb4_general_ci) as task_name,
               r.report_name,
               r.file_path,
               r.file_size,
               t.plan_start_time,
               t.plan_end_time,
               t.manager_name,
               t.operator_names,
               r.create_by,
               r.create_time,
               r.remark
        from fire_report_record r
        left join fire_maintenance_task t on t.task_id = r.task_id
    </sql>

    <select id="selectFireReportRecordList" parameterType="FireReportRecord" resultMap="FireReportRecordResult">
        <include refid="selectFireReportRecordVo"/>
        <where>  
            <if test="taskId != null "> and r.task_id = #{taskId}</if>
            <!-- Unify collations: report.* is utf8mb4_general_ci, task.task_name is utf8mb4_0900_ai_ci -->
            <if test="taskName != null  and taskName != ''">
                and (
                    convert(ifnull(r.task_name, '') using utf8mb4) collate utf8mb4_general_ci like concat('%', #{taskName}, '%') collate utf8mb4_general_ci
                    or convert(ifnull(t.task_name, '') using utf8mb4) collate utf8mb4_general_ci like concat('%', #{taskName}, '%') collate utf8mb4_general_ci
                )
            </if>
            <!-- Keyword only matches display report title (task name), not file report_name -->
            <if test="reportName != null  and reportName != ''">
                and convert(
                    ifnull(nullif(r.task_name, ''), convert(t.task_name using utf8mb4) collate utf8mb4_general_ci)
                    using utf8mb4
                ) collate utf8mb4_general_ci like concat('%', #{reportName}, '%') collate utf8mb4_general_ci
            </if>
            <if test="params.scopeUserId != null">
                and exists (
                    select 1 from fire_maintenance_task st
                    where st.task_id = r.task_id
                      and (
                        st.manager_id = #{params.scopeUserId}
                        or st.executor_id = #{params.scopeUserId}
                        or find_in_set(#{params.scopeUserId}, st.operator_ids)
                        or exists (
                            select 1 from fire_user_company uc
                            where uc.user_id = #{params.scopeUserId}
                              and uc.company_id = st.company_id
                              and (uc.status is null or uc.status = '0')
                              and uc.role_type in ('1','2','3')
                        )
                      )
                )
            </if>
        </where>
        order by r.create_time desc
    </select>
    
    <select id="selectFireReportRecordById" parameterType="Long" resultMap="FireReportRecordResult">
        <include refid="selectFireReportRecordVo"/>
        where r.report_id = #{reportId}
    </select>
        
    <insert id="insertFireReportRecord" parameterType="FireReportRecord" useGeneratedKeys="true" keyProperty="reportId">
        insert into fire_report_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="taskId != null">task_id,</if>
            <if test="taskName != null">task_name,</if>
            <if test="reportName != null and reportName != ''">report_name,</if>
            <if test="filePath != null and filePath != ''">file_path,</if>
            <if test="fileSize != null">file_size,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="taskId != null">#{taskId},</if>
            <if test="taskName != null">#{taskName},</if>
            <if test="reportName != null and reportName != ''">#{reportName},</if>
            <if test="filePath != null and filePath != ''">#{filePath},</if>
            <if test="fileSize != null">#{fileSize},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateFireReportRecord" parameterType="FireReportRecord">
        update fire_report_record
        <trim prefix="SET" suffixOverrides=",">
            <if test="taskId != null">task_id = #{taskId},</if>
            <if test="taskName != null">task_name = #{taskName},</if>
            <if test="reportName != null and reportName != ''">report_name = #{reportName},</if>
            <if test="filePath != null and filePath != ''">file_path = #{filePath},</if>
            <if test="fileSize != null">file_size = #{fileSize},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where report_id = #{reportId}
    </update>

    <delete id="deleteFireReportRecordById" parameterType="Long">
        delete from fire_report_record where report_id = #{reportId}
    </delete>

    <delete id="deleteFireReportRecordByIds" parameterType="String">
        delete from fire_report_record where report_id in 
        <foreach item="reportId" collection="array" open="(" separator="," close=")">
            #{reportId}
        </foreach>
    </delete>
    
</mapper>
