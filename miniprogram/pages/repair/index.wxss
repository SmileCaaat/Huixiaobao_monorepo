@import "../../styles/biz.wxss";
.page { display: flex; flex-direction: column; min-height: 100vh; }
.stats { display: flex; background: #fff; padding: 20rpx 0; margin-bottom: 8rpx; }
.stat-item { flex: 1; text-align: center; }
.stat-item .num { display: block; font-size: 36rpx; font-weight: 600; color: #e53935; }
.stat-item .lbl { font-size: 22rpx; color: #999; }
.list { flex: 1; height: 0; }
.line2 { margin-top: 8rpx; }
